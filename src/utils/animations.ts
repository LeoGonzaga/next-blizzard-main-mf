import { keyframes } from "styled-components";

export const FadeInAnimation = keyframes`  
  from { opacity: 0; }
  to { opacity: 1; }
`;

export const UpDownAnimation = keyframes`  
  from { bottom: 100px; }
  to { bottom: 0px; }
`;

export const ParticlesAnimation = keyframes`  
  from { top: 100px; }
  to { top: 0px; }
`;
